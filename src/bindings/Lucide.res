module Github = {
  @module("lucide-react") @react.component
  external make: (
    ~color: string=?,
    ~size: int=?,
    ~strokeWidth: float=?,
    ~className: string=?,
  ) => React.element = "Github"
}

module Linkedin = {
  @module("lucide-react") @react.component
  external make: (
    ~color: string=?,
    ~size: int=?,
    ~strokeWidth: float=?,
    ~className: string=?,
  ) => React.element = "Linkedin"
}

module Code2 = {
  @module("lucide-react") @react.component
  external make: (
    ~color: string=?,
    ~size: int=?,
    ~strokeWidth: float=?,
    ~className: string=?,
  ) => React.element = "Code2"
}

module Database = {
  @module("lucide-react") @react.component
  external make: (
    ~color: string=?,
    ~size: int=?,
    ~strokeWidth: float=?,
    ~className: string=?,
  ) => React.element = "Database"
}

module MonitorDot = {
  @module("lucide-react") @react.component
  external make: (
    ~color: string=?,
    ~size: int=?,
    ~strokeWidth: float=?,
    ~className: string=?,
  ) => React.element = "MonitorDot"
}

module ArrowRight = {
  @module("lucide-react") @react.component
  external make: (
    ~color: string=?,
    ~size: int=?,
    ~strokeWidth: float=?,
    ~className: string=?,
  ) => React.element = "ArrowRight"
}

module ArrowLeft = {
  @module("lucide-react") @react.component
  external make: (
    ~color: string=?,
    ~size: int=?,
    ~strokeWidth: float=?,
    ~className: string=?,
  ) => React.element = "ArrowLeft"
}

module PawPrint = {
  @module("lucide-react") @react.component
  external make: (
    ~color: string=?,
    ~size: int=?,
    ~strokeWidth: float=?,
    ~className: string=?,
  ) => React.element = "PawPrint"
}

module Newspaper = {
  @module("lucide-react") @react.component
  external make: (
    ~color: string=?,
    ~size: int=?,
    ~strokeWidth: float=?,
    ~className: string=?,
  ) => React.element = "Newspaper"
}

module Mail = {
  @module("lucide-react") @react.component
  external make: (
    ~color: string=?,
    ~size: int=?,
    ~strokeWidth: float=?,
    ~className: string=?,
  ) => React.element = "Mail"
}